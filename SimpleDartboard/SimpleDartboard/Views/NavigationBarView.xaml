<UserControl x:Class="SimpleDartboard.Views.NavigationBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:SimpleDartboard.Views"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Border BorderBrush="DarkSlateGray" BorderThickness="0,0,2,0">
            <DockPanel>
                <StackPanel>
                    <Button Content="Neues Spiel" Command="{Binding StartNewGameCommand}" />
                </StackPanel>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>