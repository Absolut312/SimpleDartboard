<UserControl x:Class="SimpleDartboard.Views.PlayerScoreBoardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:SimpleDartboard.Views"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <DockPanel>
            <StackPanel Orientation="Vertical">
                <StackPanel>
                    <ListView ItemsSource="{Binding ScoreActions}" Height="200">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Label Content="{Binding ActionIndex}" />    
                                    <Label Content="{Binding Score}" />    
                                    <Label Content="Punkte-Treffer" />    
                                </StackPanel>
                                
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>
                <StackPanel>
                    <Label Content="{Binding Name}" />
                    <Label Content="{Binding CurrentScore}" FontSize="25" FontWeight="Bold" />
                </StackPanel>
            </StackPanel>
        </DockPanel>
    </Grid>
</UserControl>