<UserControl x:Class="SimpleDartboard.Views.DartBoardScoreInputView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:SimpleDartboard.Views"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <DockPanel>
                <Button Content="{Binding SelectedScoreAction}" VerticalAlignment="Center"
                        Command="{Binding AddScoreActionForSelectedPlayerCommand}" Width="70" Height="70"/>
        </DockPanel>
    </Grid>
</UserControl>