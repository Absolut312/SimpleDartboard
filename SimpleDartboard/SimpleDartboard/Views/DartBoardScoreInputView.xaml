<UserControl x:Class="SimpleDartboard.Views.DartBoardScoreInputView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:SimpleDartboard.Views"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <DockPanel>
            <StackPanel Orientation="Horizontal">
                <StackPanel Orientation="Horizontal">
                    <Label Content="Punkte:" VerticalAlignment="Center" />
                    <TextBox Margin="10,0,0,0" VerticalAlignment="Center" Text="{Binding SelectedScoreAction}" MinWidth="50" />
                </StackPanel>
                <Button Margin="25,0,0,0" Content="Bestätigen" VerticalAlignment="Center"
                        Command="{Binding AddScoreActionForSelectedPlayerCommand}" />
            </StackPanel>
        </DockPanel>
    </Grid>
</UserControl>